
export const styles = theme => ({
    fabContainer: {
        position: 'sticky',
        bottom: theme.spacing.unit * 2,
        right: theme.spacing.unit * 2,
        width: '50px',
        textAlign: 'right',
        float: 'right',
    },
});
